# 운영 연동 가이드

## 1. Google 로그인

현재 웹에는 Firebase Authentication을 이용한 Google 로그인 연결 지점이 포함되어 있습니다.

1. [Firebase Console](https://console.firebase.google.com/)에서 프로젝트를 만듭니다.
2. 프로젝트 설정에서 웹 앱을 추가하고 제공되는 `firebaseConfig` 값을 복사합니다.
3. Firebase Authentication → Sign-in method에서 **Google**을 활성화합니다.
4. Authentication → Settings → Authorized domains에 `kwonsoobin63.github.io`를 추가합니다.
5. `config.js`의 `firebase` 항목에 `apiKey`, `authDomain`, `projectId`, `appId`를 넣고 다시 배포합니다.

Firebase 웹 설정값은 공개 가능한 식별자이지만, **서비스 계정 키·클라이언트 secret은 GitHub에 절대 올리면 안 됩니다.** 이메일 입력으로 계속하기는 현재 시제품의 로컬 브라우저 세션이며 실제 인증이 아닙니다.

## 2. 무아레 기반 의류 분석

### 이 HTML만으로 가능한 것

- 카메라 권한 요청과 사진 캡처
- Canvas/WebGL로 격자(정현파 프린지) 표시
- 기준/현재 사진의 업로드, 화면 가이드, 결과 화면 표시

### 별도로 필요한 것

정량적인 3D 변형도는 브라우저 UI만으로 완성되지 않습니다. 아래 구성 요소를 추가해야 합니다.

| 구성 | 역할 |
| --- | --- |
| 고정 카메라 + 소형 프로젝터/격자 광원 | 위상 이동된 정현파 패턴을 옷에 투영 |
| 보정 절차 | 체커보드로 카메라·프로젝터의 위치와 렌즈 왜곡을 보정 |
| 분석 엔진 | 4단계 위상 계산, 위상 언랩, 삼각측량, 기준·현재 메쉬 정합 |
| 2D 비전 모델 | 얼룩·색 바램·보풀처럼 높이 변화가 작은 손상 판정 |
| 백엔드/저장소 | 사진, 보정값, 분석 신뢰도, 결과와 동의 기록을 안전하게 저장 |

권장 구조는 `웹 카메라/촬영 UI → 업로드 API → Python OpenCV/구조광 분석 워커 → 결과 API → 웹`입니다. 초기 서비스에서는 2D 외관 분석을 기본으로 하고, 신뢰도 높은 3D 판정은 고정형 촬영 키오스크 또는 대여 가능한 촬영 키트에서 제공하는 편이 현실적입니다.

### 60% 판정 전 주의

60%는 제품 정책의 임계값일 뿐 광학적으로 정해진 값이 아닙니다. 실제 중고 거래 검수 결과를 모아 소재·색상·의류 종류별 가중치와 임계값을 검증해야 합니다. 광택 소재, 검은색, 투명 소재, 복잡한 프린트와 느슨한 주름은 프린지 대비를 낮춰 품질 경고 또는 재촬영이 필요합니다.
